import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    // paddingVertical: '3%',
    borderRadius: 0,
    justifyContent: 'center',
    // backgroundColor: 'red',
  },
});
